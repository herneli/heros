import DocumentCard from "../DocumentCard";
import Folder from "./Folder";

const registry = {
    FullScreen: Folder,
    Card: DocumentCard,
    emoji: "📁",
    name: "Carpeta",
};

export default registry;
